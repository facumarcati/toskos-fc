<div class="page-header">
  <span class="eyebrow">
    Perfil de jugador
  </span>
  <h1 class="player-name" id="playerNameWrapper">
    <span id="playerNameText">
      {{player.name}}
    </span>
    <button
      class="btn-edit-name"
      id="editNameBtn"
      data-player="{{player._id}}"
    >
      ✏️
    </button>
  </h1>
  {{#if player.user}}
  <div class="player-username">
    <span id="usernameText">
      @{{player.user.username}}
    </span>
    {{#if (eq player.user._id currentUser._id)}}
    <button
      class="btn-edit-username"
      id="editUsernameBtn"
      data-player="{{player._id}}"
    >
      ✏️
    </button>
    {{/if}}
  </div>
  {{/if}}
  <label class="filter-checkbox" style="margin-top:8px; display:inline-flex;">
    <input
      type="checkbox"
      id="toggleGuest"
      {{#if player.guest}}checked{{/if}}
      onchange="updateGuestStatus('{{player._id}}', this.checked)"
    />
    Jugador invitado
  </label>
</div>

<div class="matches-header">
  <form method="GET" class="season-filter">
    <select name="season" onchange="this.form.submit()">
        <option value="all" {{#if (eq selectedSeason "all")}}selected{{/if}}>Todas las temporadas</option>
        <option value="2026" {{#if (eq selectedSeason "2026")}}selected{{/if}}>2026</option>
        <option value="2025" {{#if (eq selectedSeason "2025")}}selected{{/if}}>2025</option>
        <option value="2024" {{#if (eq selectedSeason "2024")}}selected{{/if}}>2024</option>
    </select>
  </form>
</div>

<div class="profile-layout">
  <div class="form-panel">
    <p class="section-label">
      Estadísticas
    </p>
    <div class="stats-grid">
      <div class="stat-box">
        <strong>{{stats.played}}</strong>
        <span>Partidos</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.winrate}}%</strong>
        <span>Winrate</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.goals}}</strong>
        <span>Goles</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.assists}}</strong>
        <span>Asist</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.ga}}</strong>
        <span>G+A</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.goalsPerMatch}}</strong>
        <span>Gol / P</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.gaPerMatch}}</strong>
        <span>G+A / P</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.wins}}</strong>
        <span>Victorias</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.draws}}</strong>
        <span>Empates</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.losses}}</strong>
        <span>Derrotas</span>
      </div>
    </div>
  </div>
</div>

<div class="profile-teams">
  <div class="form-panel">
    <p class="section-label">
      Equipo blanco
    </p>
    <div class="team-grid">
      <div class="stat-box">
        <strong>{{stats.white.played}}</strong>
        <span>PJ</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.white.wins}}</strong>
        <span>PG</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.white.draws}}</strong>
        <span>PE</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.white.losses}}</strong>
        <span>PP</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.white.winrate}}%</strong>
        <span>WR</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.white.goals}}</strong>
        <span>G</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.white.assists}}</strong>
        <span>A</span>
      </div>
    </div>
  </div>
  <div class="form-panel">
    <p class="section-label">
      Equipo negro
    </p>
    <div class="team-grid">
      <div class="stat-box">
        <strong>{{stats.black.played}}</strong>
        <span>PJ</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.black.wins}}</strong>
        <span>PG</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.black.draws}}</strong>
        <span>PE</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.black.losses}}</strong>
        <span>PP</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.black.winrate}}%</strong>
        <span>WR</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.black.goals}}</strong>
        <span>G</span>
      </div>
      <div class="stat-box">
        <strong>{{stats.black.assists}}</strong>
        <span>A</span>
      </div>
    </div>
  </div>
</div>

{{#if trophies.length}}
<div class="profile-trophies">
  <div class="form-panel">
    <p class="section-label">Títulos</p>
    <div class="trophy-list">
      {{#each trophies}}
      <div class="trophy-card" data-position="{{this.position}}">
        <span class="trophy-medal">{{this.medal}}</span>
        <div class="trophy-info">
          <span class="trophy-title">{{this.type}} {{this.season}}</span>
          <span class="trophy-value">{{this.value}} {{this.unit}}</span>
        </div>
      </div>
      {{/each}}
    </div>
  </div>
</div>
{{/if}}

<div class="player-matches">
  <div class="section-header">
    <h2 id="matchesTitle">Partidos recientes</h2>
  </div>
  <div id="matchesContainer">
    {{#each matches}}
    <div class="match-card match-card-compact">
        <div class="match-scoreboard
        {{#if (gt this.teamA this.teamB)}}scoreboard-win-a
        {{else if (gt this.teamB this.teamA)}}scoreboard-win-b
        {{else}}scoreboard-draw
        {{/if}}">
          <div class="match-team">
              <span class="match-team-label">Equipo</span>
              <span class="match-team-name">Blanco</span>
          </div>
          <div class="match-score">
              <div class="match-date">
              {{formatDate this.date}} · {{this.venue}}
              </div>
              <span class="match-score-number">
              {{this.teamA}} — {{this.teamB}}
              </span>
          </div>
          <div class="match-team match-team-right">
              <span class="match-team-label">Equipo</span>
              <span class="match-team-name">Negro</span>
          </div>
        </div>
        <div class="match-card-footer match-card-footer-compact">
        <div class="footer-left">
            {{#if (gt this.teamA this.teamB)}}
            {{#if (eq this.playerStats.team "A")}}
                <span class="match-badge badge-win">Victoria</span>
            {{else}}
                <span class="match-badge badge-loss">Derrota</span>
            {{/if}}
            {{else if (gt this.teamB this.teamA)}}
            {{#if (eq this.playerStats.team "B")}}
                <span class="match-badge badge-win">Victoria</span>
            {{else}}
                <span class="match-badge badge-loss">Derrota</span>
            {{/if}}
            {{else}}
            <span class="match-badge badge-draw">Empate</span>
            {{/if}}
            <span class="player-stat {{#if this.playerStats.goals}}stat-active{{/if}}">
                ⚽ {{this.playerStats.goals}}
            </span>
            <span class="player-stat {{#if this.playerStats.assists}}stat-active{{/if}}">
                🎯 {{this.playerStats.assists}}
            </span>
        </div>
        <div class="footer-right">
            <a
            href="/matches?season=all#match-{{this._id}}"
            class="btn-match-action btn-edit"
            >
            Ver
            </a>
        </div>
        </div>
    </div>
    {{else}}
        <div class="list-empty-state">
        Sin partidos todavía
        </div>
    {{/each}}
  </div>
  {{#if (gt totalMatches 3)}}
  <button
    id="loadMoreMatches"
    class="btn-yellow"
    data-page="1"
    data-player="{{player._id}}"
    data-season="{{selectedSeason}}"
  >
    Ver más
  </button>
  {{/if}}
</div>

<script>
  window.currentUser = {{{json currentUser}}};
</script>
<script src="/js/playerProfile.js"></script>